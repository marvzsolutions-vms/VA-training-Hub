# V2.8 Appearance

- Branding contains the academy default: Light or Dark.
- Every user can choose Light or Dark in Profile.
- The personal setting overrides the academy default only for that user.
- Run `RUN_THIS_IN_SUPABASE_SQL_EDITOR.sql`, then `npm install`, `npm run dev`, and `npm run build`.
