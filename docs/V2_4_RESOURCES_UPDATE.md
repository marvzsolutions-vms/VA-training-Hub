# V2.4 Resources update

The Resources page now includes an **Add resource** button for owners, managers, and coaches. Staff can add unlimited resource cards. Students remain view-only and only see active resources that contain content.

Run the appended V2.4 section in `RUN_THIS_IN_SUPABASE_SQL_EDITOR.sql` to remove the old six-slot database limit.
